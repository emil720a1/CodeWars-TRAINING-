function sumOfMinimums(arr) {
let a = 0
arr.forEach(student => {
    a+=Math.min(...student);
})
console.log(a)
return a
}



//Этот же код через reduce()

function sumOfMinimums(arr) {
  return arr.reduce((p, c) => p + Math.min(...c), 0);
}
// Это короткая версия(Не мой код)
